version: 2

sources:
  - name: sravan
    schema: thelook_ecommerce
    tables:
      - name: ord_items
        identifier: order_items
        loaded_at_field: created_at
        freshness: 
          error_after:
            count: 2
            period: day
          warn_after:
            count: 1
            period: hour
        