version: 2

models:
  - name: stg_orders
    columns:
      - name: id
        tests:
          - unique
          - not_null
  - name: stg_products
    columns:
      - name: category
        tests:
          - accepted_values:
              values: ['Accessories','Plus']
 
